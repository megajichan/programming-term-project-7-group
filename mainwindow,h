#ifndef MAINWINDOW_H
#define MAINWINDOW_H

#include <QMainWindow>
#include <QPixmap>
#include "termproject.h"
#include "logicsimwidget.h"

QT_BEGIN_NAMESPACE
namespace Ui { class MainWindow; }
QT_END_NAMESPACE

class MainWindow : public QMainWindow
{
    Q_OBJECT

public:
    MainWindow(QWidget *parent = nullptr);
    ~MainWindow();

private slots:
    void on_btnBinarize_clicked();
    void on_btnInvert_clicked();
    void on_btnFlipLR_clicked();
    void on_btnFlipUD_clicked();
    void on_btnSave_clicked();
    void on_btnReset_clicked();

protected:
    void paintEvent(QPaintEvent *event) override;

private:
    Ui::MainWindow  *ui           = nullptr;
    termproject     *myTermObject = nullptr;
    LogicSimWidget  *simWidget    = nullptr;
    QPixmap          bgImage;
    void displayImage();
};

#endif // MAINWINDOW_H
