#ifndef MAINWINDOW_H
#define MAINWINDOW_H

#include <QMainWindow>
#include <QLabel>
#include <QPushButton>
#include "customimage.h" // 커스텀 이미지 클래스 포함

class MainWindow : public QMainWindow
{
    Q_OBJECT

public:
    MainWindow(QWidget *parent = nullptr);
    ~MainWindow();

private slots:
    void onProcessButtonClicked(); // 버튼 클릭 시 자체 C++ 클래스 제어

private:
    QLabel *imageLabel;
    QPushButton *processButton;
    
    CustomImage myCppImage; // 순수 C++ 이미지 클래스 객체
    
    void displayCustomImage(); // CustomImage를 QImage로 변환해 화면에 띄우는 함수
};

#endif // MAINWINDOW_H
